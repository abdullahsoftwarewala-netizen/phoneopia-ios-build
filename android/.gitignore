# Android signing — never commit
key.properties
*.jks
*.keystore

# Machine-local SDK paths (auto-generated by Flutter/Android Studio)
local.properties

# Gradle / Kotlin build cache
.gradle/
.kotlin/
build/

# JVM crash dumps (not malware — delete & ignore)
hs_err_pid*.log
replay_pid*.log